{{!--
  Page body. The left nav sidebar is intentionally omitted: this site has no
  versioned releases and only a handful of top-level sections, all reachable
  from the header bar (see header.hbs). Only the main article column is
  rendered.
--}}
<main class="content">
  <article class="doc">
    {{> breadcrumbs}}
    {{> article}}
  </article>
</main>
