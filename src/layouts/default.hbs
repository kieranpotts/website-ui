<!DOCTYPE html>
<html lang="{{or site.lang 'en'}}">
  <head>
    {{> head}}
  </head>
  <body>
    <main class="MAIN">
      {{> header}}
      {{> body}}
      {{> footer}}
    </main>
  </body>
</html>
