<!DOCTYPE html>
<html lang="{{or site.lang 'en'}}">
  <head>
    {{> head}}
  </head>
  <body>
    {{> header}}
    <main class="MAIN">
      {{> toc}}
      {{> breadcrumbs}}
      {{> body}}
      {{> footer}}
    </main>
  </body>
</html>
